<nav class="bg-gray-900 border-b border-gray-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            <div class="flex items-center">
                <a href="/" class="flex items-center text-cyan-400 hover:text-cyan-300 transition-colors duration-200">
                    <i class="fas fa-shield-alt text-xl mr-2"></i>
                    <span class="font-bold text-lg">Siber Güvenlik Akademi</span>
                </a>
                <% if (user) { %>
                    <div class="hidden md:block ml-10">
                        <div class="flex items-baseline space-x-4">
                            <a href="/dashboard" class="text-gray-300 hover:text-cyan-400 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                                <i class="fas fa-chart-line mr-1"></i> Dashboard
                            </a>
                            <a href="/pathways" class="text-gray-300 hover:text-cyan-400 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                                <i class="fas fa-road mr-1"></i> Kariyer Yolları
                            </a>
                        </div>
                    </div>
                <% } %>
            </div>

            <div class="flex items-center">
                <% if (user) { %>
                    <div class="flex items-center space-x-4">
                        <span class="text-gray-300 flex items-center">
                            <i class="fas fa-user-circle text-xl mr-2"></i>
                            <span class="text-sm font-medium"><%= user.full_name %></span>
                        </span>
                        <a href="/logout" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                            <i class="fas fa-sign-out-alt mr-1"></i> Çıkış
                        </a>
                    </div>
                <% } else { %>
                    <div class="flex items-center space-x-4">
                        <a href="/login" class="text-gray-300 hover:text-cyan-400 px-4 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                            <i class="fas fa-sign-in-alt mr-1"></i> Giriş
                        </a>
                        <a href="/register" class="bg-cyan-600 hover:bg-cyan-700 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                            <i class="fas fa-user-plus mr-1"></i> Kayıt Ol
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</nav> 